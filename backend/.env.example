PORT=3000
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cafe_pos?schema=public"
# WAJIB, minimal 16 karakter — generate yang panjang & acak.
JWT_SECRET="cafe_pos_super_secret_jwt_key_2026"

# Kredensial seed (`npm run prisma:seed` / docker entrypoint). TANPA password
# default hardcode — kedua password wajib diisi sebelum seed bisa jalan.
SEED_ADMIN_USERNAME=admin
SEED_ADMIN_PASSWORD=
SEED_CASHIER_USERNAME=kasir1
SEED_CASHIER_PASSWORD=
